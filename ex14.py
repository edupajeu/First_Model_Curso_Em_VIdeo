c = float(input('Inform the temperature in Celsius:  '))
f = ((9*c)/5)+32
# we can use this formula without ()
print('The temperature {}ºC was converted to {}ºF '.format(c, f))
# to define which field comes first we can use numbers inside {} to get the right order
